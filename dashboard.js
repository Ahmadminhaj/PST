const $=id=>document.getElementById(id),toast=$("toast");function showToast(m){toast.textContent=m;toast.classList.add("show");clearTimeout(window.t);window.t=setTimeout(()=>toast.classList.remove("show"),1800)}$("homeBtn").onclick=()=>location.href="index.html";const modal=$("addModal");$("addBtn").onclick=()=>modal.classList.add("open");$("closeModal").onclick=()=>modal.classList.remove("open");let documents=JSON.parse(localStorage.getItem("psd_documents")||"[]");function save(){localStorage.setItem("psd_documents",JSON.stringify(documents))}function esc(v){return String(v).replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#039;"}[m]))}function date(v){return v?new Date(v+"T00:00:00").toLocaleDateString("en-IN",{day:"2-digit",month:"short",year:"numeric"}):"-"}function render(){const list=$("documentList");list.innerHTML="";let total=0,due=0;const today=new Date();today.setHours(0,0,0,0);documents.forEach((d,i)=>{total+=Number(d.amount)||0;const days=Math.ceil((new Date(d.date+"T00:00:00")-today)/86400000);if(days>=0&&days<=7)due++;const c=document.createElement("div");c.className="document-card";c.innerHTML=`<div class="doc-icon">📄</div><div class="doc-info"><strong>${esc(d.name)}</strong><small>Due: ${date(d.date)}</small></div><div class="doc-amount"><strong>₹${Number(d.amount||0).toFixed(0)}</strong><small>Active</small></div><button class="delete-btn" data-i="${i}">×</button>`;list.appendChild(c)});$("totalCount").textContent=documents.length;$("activeCount").textContent=documents.length;$("dueCount").textContent=due;$("monthlyTotal").textContent="₹"+total.toFixed(0);$("emptyState").style.display=documents.length?"none":"block";document.querySelectorAll(".delete-btn").forEach(b=>b.onclick=()=>{documents.splice(+b.dataset.i,1);save();render();showToast("Document removed.")})}$("saveDoc").onclick=()=>{const name=$("docName").value.trim(),amount=$("docAmount").value,dateVal=$("docDate").value;if(!name||!dateVal){showToast("Enter document name and due date.");return}documents.push({name,amount:amount||0,date:dateVal});save();render();modal.classList.remove("open");$("docName").value=$("docAmount").value=$("docDate").value="";showToast("Document added.")};render();
